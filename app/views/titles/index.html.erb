<p id="notice"><%= notice %></p>

<h1>Listing Titles</h1>

<table>
  <thead>
    <tr>
     
      <th>Title</th>
	  <th>Count</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @titles.each do |title| %>
      <tr>
        
        <td><%= title.title %></td>
		<td><%= link_to Title.where(title: title.title).count, titemp_url(id: ) %></td>
    link_to empcount, emp_url(id: department.dept_no)		
       
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Title', new_title_path %>
